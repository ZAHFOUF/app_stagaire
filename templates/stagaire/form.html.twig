  {% extends 'base.html.twig' %}
  
  
  {% block body %}

  <h2 class="text-center p-5"> {{type}} un Stagaire </h2>

{{ form_start(form) }}

{{ form_widget(form) }}
  

  <button type="submit" class="btn btn-primary btn-block mb-4"> ajouter </button>

  {{ form_end(form) }}

 
       
        
        
   {% endblock %}